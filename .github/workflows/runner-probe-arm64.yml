name: Runner Probe ARM64

on:
  workflow_dispatch:

jobs:
  probe:
    runs-on: ubuntu-24.04-arm

    steps:
      - name: Show runner architecture
        run: |
          echo "runner.os=$RUNNER_OS"
          echo "runner.arch=$RUNNER_ARCH"
          uname -a
          uname -m
